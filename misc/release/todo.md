* produce a data.json that lives in the root of the AWS_S3_PKGS_BUCKET
* all build output logged in AWS_S3_PKGS_BUCKET/data.json
* the site and everything else should also log in AWS_S3_PKGS_BUCKET/data.json